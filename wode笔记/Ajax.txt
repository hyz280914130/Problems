json：{}里面是对象以键值对形式存在，[]里面是数组，一个[]里面可以放多个{},一个{}里面也可以放多个[]，反正{}、[]可以相互嵌套

异步：客户端将请求交给ajax，然后ajax再将请求给tomcat服务器，相当于ajax是一个代理，所以才能实现异步

new ColumnListHandler("pname") 只将pname这一列存入List

后端将结果使用json格式传，前端使用ajax接受

不懂得地方去w3c 查看