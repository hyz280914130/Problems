1.居中标签
  <center> 内容 </center> ： 是内容居中显示，相当于 align属性center值。
  
2.表格的标题标记和表头标记
  标题标记：<caption></caption>
  表头标记：<th></th>
  
3.form表单的target属性
  _black : 在新窗口打开
  _top : 在整个浏览器打开
  _parent ： 在上一级窗口打开
  _self ：在同一个窗口打开
  
4.select标签的multiple属性
  使下拉列表实现多选
  
5.form表单的multiple属性
  使表单支持多内容，如果要让表单实现文件上传就必须使用这个标签，表单在提交时会根据内容的不同，在请求体中分成不同的部分，
  然后在后台代码中判断上传的是一般内容，还是文件，如果是文件的话，书写代码接收。

6.<textarea>多行文本标记
  可以实现多行文本显示，可以通过rows、cols来规定行数和列数，此外还可以通过wrap属性，来决定是否自动换行。
  
7.valign属性
  垂直对齐，middle值表示居中。
  
8.HTML5新增的article标签
  它是一个与上下文无关的，独立的一个区域，有自己的<header>和<footer>
  
9.HTML5中新增的input属性
  email : 必须输入email格式的内容
  url ： 必须输入url
  nubmer ：必须输入数字
  range ： 必须输入一定数值范围的文本框
  
