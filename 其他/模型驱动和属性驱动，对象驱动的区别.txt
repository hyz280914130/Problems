	访问一个action就会创建一个action实例
	属性驱动和对象驱动的都是在访问这个action之后，值栈把这个action的实例放到栈顶，有一个params拦截器会根据action中的属性进行将请求参数进行封装
	模型驱动中没有属性，只有一个user方法，在sruts中的params（参数拦截器，把数据封装成action对应的属性值）拦截器之前有一个ModerDriven拦截器，它判断出action是否实现了ModelDriven->如果实现获取值栈->调用getModel方法获取model->把我们用来封装属性的对象压到栈顶
